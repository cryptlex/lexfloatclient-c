# LexFloatClient Android Example

## Run

Open `examples/sample` in Android Studio and run the `app` module,
or build from the command line:

```bash
cd examples/sample
gradle assembleDebug
```

Set your product ID and float server URL in `MainActivity.java` before running.
